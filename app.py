from fastapi import FastAPI from pydantic import BaseModel from pathlib import Path from tempfile import NamedTemporaryFile from engines.semantic_tether_engine import build_traceable_output app = FastAPI() class Input(BaseModel): text: str @app.post("/analyze") def analyze(data: Input): with NamedTemporaryFile(mode="w", suffix=".txt", delete=False, encoding="utf-8") as tmp: tmp.write(data.text) tmp_path = Path(tmp.name) try: result = build_traceable_output(tmp_path) return result finally: if tmp_path.exists(): tmp_path.unlink()
